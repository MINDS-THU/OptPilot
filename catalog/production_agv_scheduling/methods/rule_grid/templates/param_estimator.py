"""Compatibility module for policies that do not estimate trace parameters.

The rule baselines are fully specified by their three rule names, so importing
this module performs no database access and has no side effects.
"""

AVERAGE_ORDER_INTERVAL = 10.0
