# Pruned COOPA runtime for the coopa-solver method.
# This is the ~10-package subset the paper pipeline actually uses, extracted
# from the 40+-pin research requirements; the retrieval/KB/web tail
# (crawl4ai, gradio, langchain, e2b, docker-py, serpapi) is deliberately
# dropped — the shim never imports COOPA's web or knowledge agents.
#
# Install into the SAME Python environment that runs `optpilot` / Studio:
#   uv pip install -r catalog/or_solving/methods/coopa_solver/requirements-pruned.txt
#
# Solver backends are user-provisioned extras: `ortools` and `pymoo` below
# are pip-installable; GLPK and IPOPT binaries come from your system package
# manager (e.g. `brew install glpk ipopt`). Without them, use
# agentMode: mathematical-only where Pyomo + GLPK suffice.

smolagents==1.24.0  # COOPA's own pin; 1.26 drifts agent APIs
litellm
instructor==1.9.2  # pre-registry instructor; 1.15+ rejects COOPA's OPENROUTER structured-output mode
pydantic
pyomo
ortools
pymoo
numpy
scipy
tenacity
python-dotenv

# Import-time closure only: COOPA's optimizer agents import the web-browsing
# and document-inspection modules at module load, so these light packages
# must be importable even though the pruned shim never invokes them (the
# heavy crawl/UI tail — crawl4ai, gradio, langchain, e2b — stays excluded).
google-search-results
pathvalidate
mammoth
markdownify
pdfminer.six
python-pptx
openpyxl
puremagic
pydub
SpeechRecognition
youtube-transcript-api
