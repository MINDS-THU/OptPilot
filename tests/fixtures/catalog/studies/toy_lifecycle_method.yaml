apiVersion: optpilot.io/v3alpha1
kind: StudyConfig
name: toy-lifecycle-method
description: Exercise a user-owned lifecycle method loaded through the python component hook
tags: [mvp, toy, user-method, lifecycle-method]

environment: ../environments/toy_factory.yaml
method: ../methods/lifecycle_fixed_parameter_method.yaml

objective:
  metric: throughput
  direction: maximize

instances:
  source: files
  paths:
    - ../instances/toy_factory_case.yaml

budget:
  maxTrials: 2

execution:
  backend: local
  parallelism: 2
  timeoutSeconds: 120

reproducibility:
  seed: 7

