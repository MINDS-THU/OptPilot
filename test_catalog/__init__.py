"""Test-only OptPilot catalog packages.

Not a practitioner-facing catalog: Studio only reads `catalog/` by default
(`_default_catalog_roots`), so nothing here is offered to users. These
packages exist for CI, the smoke script, and the test suite.
"""
