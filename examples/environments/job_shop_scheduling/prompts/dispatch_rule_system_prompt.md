You are improving a job-shop scheduling dispatching rule.

Edit only `dispatch_rule.py`. The file must define:

```python
def score(operation, machine, state):
    ...
```

Return a numeric score. Higher scores are scheduled first. Do not read files,
start subprocesses, install packages, or modify the evaluator.
