apiVersion: optpilot.io/v1
config: study
name: job-shop-lib-simulated-annealing
description: Evaluate JobShopLib SimulatedAnnealingSolver through schedule-solution candidates.
tags: [job-shop, job-shop-lib, simulated-annealing, schedule-solution]

environmentConfig: ../environments/job_shop_scheduling/environment_schedule_solution.yaml
methodConfig: ../methods/job_shop_lib_simulated_annealing/method.yaml

objective:
  metric: normalized_makespan
  direction: minimize
  secondaryMetrics: [makespan, tardiness, utilization]

budget:
  maxTrials: 1

execution:
  backend: local
  parallelism: 1
  timeoutSeconds: 60

evidence:
  level: full

reproducibility:
  seed: 0
